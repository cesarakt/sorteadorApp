import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        height: 80,
        backgroundColor: "#18505F",
        paddingHorizontal: 10,
        paddingTop: 10,
        marginBottom: 20,
        justifyContent: "center"
    },
    titulo: {
        fontSize: 20,
        color: "#11C3C7",
        fontWeight: "700"
    },
    subTitulo: {
        fontSize: 16,
        color: "#fff"
    }
});

export default styles;